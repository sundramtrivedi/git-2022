import { OPCUACertificateManager } from "node-opcua-certificate-manager";
import { TrustListDataType } from "node-opcua-types";
import { AbstractFs } from "node-opcua-file-transfer";
export declare enum TrustListMasks {
    None = 0,
    TrustedCertificates = 1,
    TrustedCrls = 2,
    IssuerCertificates = 4,
    IssuerCrls = 8,
    All = 15
}
export declare function buildTrustList(certificateManager: OPCUACertificateManager, trustListFlag: TrustListMasks): Promise<TrustListDataType>;
export declare function writeTrustList(fs: AbstractFs, filename: string, trustListFlag: TrustListMasks, certificateManager: OPCUACertificateManager): Promise<void>;
